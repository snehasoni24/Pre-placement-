#include <stdio.h>
#define N 3
int main(){
    int a[N][N]={{1,2,3},{2,5,6},{3,6,9}}, flag=1;
    for(int i=0;i<N;i++)
        for(int j=0;j<N;j++)
            if(a[i][j] != a[j][i]) flag=0;
    printf(flag?"Symmetric":"Not Symmetric");
}
