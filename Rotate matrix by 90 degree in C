#include <stdio.h>
#define N 3
int main() {
    int a[N][N] = {{1,2,3},{4,5,6},{7,8,9}}, b[N][N];
    for(int i=0;i<N;i++)
        for(int j=0;j<N;j++)
            b[j][N-1-i] = a[i][j];
    for(int i=0;i<N;i++){
        for(int j=0;j<N;j++) printf("%d ", b[i][j]);
        printf("\n");
    }
}
