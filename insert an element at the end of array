#include <stdio.h>

void insertAtEnd(int arr[], int *n, int element) {
    arr[*n] = element;
    (*n)++;
}

int main() {
    int arr[100] = {1, 2, 3};
    int n = 3;
    insertAtEnd(arr, &n, 4);
    for (int i = 0; i < n; i++) printf("%d ", arr[i]);
    return 0;
}
