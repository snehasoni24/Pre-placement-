#include <stdio.h>
#define N 3
int main(){
    int a[N][N]={{1,0,3},{0,0,6},{7,8,0}}, count=0;
    for(int i=0;i<N;i++)
        for(int j=0;j<N;j++)
            if(a[i][j]==0) count++;
    printf("Zeros = %d",count);
}
