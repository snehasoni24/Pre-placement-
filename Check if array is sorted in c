#include <stdio.h>
#include <stdbool.h>

bool isSortedAndRotated(int arr[], int n) {
    int count = 0;
    for (int i = 0; i < n; i++)
        if (arr[i] > arr[(i + 1) % n]) count++;
    return count == 1;
}
