#include <stdio.h>
int main(){
    int a[]={1,2,3}, b[]={4,5}, m=3, n=2, prod[10]={0};
    for(int i=0;i<m;i++)
        for(int j=0;j<n;j++)
            prod[i+j]+=a[i]*b[j];
    for(int i=m+n-2;i>=0;i--) if(prod[i]) printf("%dx^%d ",prod[i],i);
}
